apiVersion: v1
kind: Service
metadata:
  name: shopinventorynpservice-srv
spec:
  type: NodePort
  selector:
    app: shopinventoryservice
  ports:
    - name: shopinventoryservice
      protocol: TCP
      port: 80
      targetPort: 80
